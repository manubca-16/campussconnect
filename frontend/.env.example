# Backend base URL for API calls (Render URL in production)
VITE_API_BASE_URL="http://localhost:3000"

# Optional (only if you actually use Gemini directly in the browser)
VITE_GEMINI_API_KEY="YOUR_KEY_HERE"

